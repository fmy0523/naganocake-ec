<h2>会員一覧</h2>

<table class="table table-striped">
  <thead>
    <tr>
     <th><P>会員ID</P></th>
     <th><P>氏名</P></th>
     <th><p>メールアドレス</p></th>
     <th><p>会員ステータス</p></th>
    </tr>
  </thead>

<!-- メンバーの苗字と名前を連結。会員ステータスの表示（有効・退会済み）をさせる。 -->

  <% @members.each do |member| %>
      <tr class="new-table">
        <td>
          <%= member.id %>
        </td>
        <td>
          <%= link_to admin_member_path(member) do %>
            <span>
              <%= member.last_name %>
              <%= member.first_name %>
            </span>
          <% end %>
        </td>
        <td>
          <%= member.email %>
        </td>
        <td>
          <%= member.is_deleted %>
        </td>

      </tr>
    <% end %>
  </tbody>
</div>